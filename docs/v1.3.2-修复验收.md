# 123mshub v1.3.2 修复验收

## 修复项

| 项目 | 结果 | 验证 |
| --- | --- | --- |
| 记忆图示首次打开 | 通过 | `MemoryGraph.vue` 为 user/project/reference/feedback 显式注册 Sigma 内置 `NodeCircleProgram`；未知业务类型回退到 reference，避免 Sigma 将业务类型误当成未注册程序 |
| 技能库和记忆库筛选栏圆角 | 通过 | `.filter-select` 负责背景、边框和圆角，关闭状态的原生 select 透明；亮暗主题展开选项仍分别使用可读配色 |
| 记忆类型中文显示 | 通过 | 统计筛选、批量分类、记忆抽屉和收编投递统一显示“用户、项目、参考、反馈”，接口值保持原有英文枚举 |

## 自动化验证

- `npm run check`：通过。
- `npm test`：7 项通过，包含 3000 节点 / 6000 边图谱性能测试。
- `npm run build`：通过，生产 UI 已复制到 `src/mshub/web`。
- `python -m pytest -q`（项目虚拟环境 + `PYTHONPATH=src`）：全套通过。
- `python -m compileall -q src`：通过。
- `scripts/build-release.ps1 -SkipFrontendBuild`：通过，生成 v1.3.2 EXE 和 ZIP。
- `scripts/verify-release.ps1`：通过，技能、记忆、提示词、日报全场景通过。
- 指定路径 `D:\DiskWork\cli\123mshub.exe` 自动读取隔离配置：通过，返回 `configured=true`、`version=1.3.2` 和预置仓库路径。
- `git diff --check`：通过；仅有 Windows 工作区的 LF/CRLF 提示。

## 发布校验

```text
123mshub.exe
1252e086291086d82977fc5bedbeb1e84b54415a5c587c395c3ed82c22d2a2b2

123mshub-v1.3.2-win64.zip
22d6de874e71a71ebddd825ebfa78e913caa720e0fde2215eca7afc3537d4bf3
```

## Windows 防护审查

本次修复只涉及 Sigma 节点程序注册、CSS 层级和中文显示，并未引入外部下载、管理员提权、服务注册、开机自启、代码混淆或新的进程执行路径。现有命令调用仍使用参数数组和隐藏窗口参数，前端依赖随静态资源打包。

当前环境没有可用的浏览器自动化会话，因此没有伪造截图结论；构建产物、接口、独立 EXE 和发布验收脚本均已验证通过。

