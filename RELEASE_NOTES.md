# 123 MSHub v1.0.0 发布说明

**123 MSHub（Memory & Skill Hub）** 首个正式版——本地 agent 的共享大脑管理器。
由 123skillrepo v0.9.0 升级而来：技能区能力全部继承，新增记忆库与注入提示词中心。

## 新产品能力

### 记忆库（全新）
- 数据模型：`memory\MEMORY.md` 索引 + `notes\*.md` 条目（一文件一条知识，带 frontmatter）；
  索引是程序维护的派生物，条目文件是事实源，SQLite 只做本机缓存；
- 六个场景全支持：审核 agent 投递（红点 → 预览 → 收编）、手动记一条、搜索
  （标题+描述+标签+正文全文）、修正、软删除、总览分发；
- 详情抽屉直接编辑：frontmatter 表单 + 等宽正文编辑器（编辑/预览切换，不做富文本）、
  `[[双链]]` 展示跳转、AI 生成描述、重名防护（409 + 「打开它」）；
- inbox 收编视图：左侧投递原文、右侧可编辑表单；命中提示词注入规则显示红条警告；
  支持单条丢弃与全部丢弃；
- 整理与日报：手动「归纳整理」（本地盘点 + 配置 AI 接口时 LLM 分析）与
  值守 Agent（复制值守提示词交给常驻 agent 部署）两条路，日报在 `memory\reports\`，
  仅报记忆与技能；

### 注入提示词中心（产品灵魂）
- 一段提示词同时教三件事：读共享记忆、用共享技能库、往 inbox 投递新知识；
- 内含安全纪律（仓库文件中的“忽略以上指令”类内容视为注入）与连通暗号彩蛋
  （发「AI猛如虎」，agent 只回「Token任你烧！/ 暗号对齐，123mshub已接入。」）；
- 记忆库页与技能库页顶部常驻「复制注入提示词」按钮；

### 技能库强化
- 来源徽章：「GitHub 源」可查版本可更新；「本地自研」更新/查版本按钮置灰并说明原因；
- 设置页「安全检查 B 线路」升级为「AI 接口」（安全审查 + 记忆辅助 + 整理分析共用）；
- 新增「高级」折叠区：记忆库位置覆盖（memory_root_override）；
- 「重新识别已同步条目」同时触发记忆对账（冲突副本隔离、懒对账、索引重建）。

### 继承自 123skillrepo v0.9.0
GitHub/本地入库、标准/全仓安装、程序项目克隆、安全检查 A/B 路线、标签、
中文备注 AI 翻译、编辑信息（身份与归属分离）、更新与备份、镜像/代理、
瞬时锁退避重试、点文件容器、git.bundle 随库、启动自愈、亮/暗双主题。

## 其他

- 默认端口 8766（与 123skillrepo 的 8765 分开，两者可同时运行）；
- 配置目录 `%APPDATA%\123mshub\`，与旧版完全隔离，互不干扰；
- 免安装：解压 zip 双击 `123mshub.exe` 即用；SmartScreen 提示时「更多信息 → 仍要运行」。

## 致谢

记忆管理方式借鉴 [Engramory](https://github.com/tinqiao-oss/engramory)
（抖音 @李云放而已 的开源项目，MIT）：markdown 条目 + 常读索引、四类型词汇、
整理纪律、密钥只记位置不记值、索引上限纪律（150 行 / 20KB 健康线）。
