# 123 MSHub v1.2.4 发布说明

> v1.2.3 基线上的缺陷修复版：修「端口占用连环弹窗」，导入器两处口径修正，记忆编辑加「放大窗口」。

## 修复

- **双开检测：提示一次即退出**：端口被占时弹一次「请勿双开：端口 8766 已被占用」即中止，
  不重试、不驻留（配合下条，弹窗轰炸的根因已除）；
- **关窗后进程残留占端口**：窗口关闭后强制退出进程，杜绝「明明关了还报端口被占用」
  的僵尸进程（此前 WebView2 线程偶发不随主线程退出）；
- **技能包模板不再误入记忆**：已认领为技能的目录，其内部模板/示例 md（典型如 engramory
  技能包的 `templates\example-*.md`）不再被散装通道当成记忆导入；
- **导入预览口径**：技能候选按「库 + 目录名」去重，预览数与实际导入结果一致
  （修复「预览 260 个、装出来 255 个」的困惑）。

## 功能

- **记忆编辑抽屉「放大编辑窗口」**：标题栏新增放大/还原按钮，窗口展至 `min(1200px, 96vw)`、
  正文编辑区最小高度提至 55vh，再点还原。仅放大几何尺寸，默认视觉与动效零改动。

## 验证

- `pytest tests`：171 项全部通过（新增 2 项导入器测试；GUI 测试更新为双开提示与关窗强退断言）；前端 `vite build` 通过。

---

# 123 MSHub v1.2.3 发布说明

> v1.2.2 基线不变，本版为连通暗号换梗版：旧梗认知门槛高，换成人人能 get 的对暗号，并播报仓库实数。

## 变更

- **连通暗号换梗**：触发词 `AI猛如虎` → `天王盖地虎`，回复由固定两行改为固定一行——
  「AI镇河妖！暗号对齐，123mshub已接入，当前仓库拥有X条记忆，X项技能。」；
- **暗号播报实数**：X 为生成注入提示词 / 值守整理提示词那一刻程序实时清点的
  记忆条数与技能数（与程序内统计同源），对上暗号顺带带出仓库规模；
- 注入提示词与值守提示词同步换梗；README、需求/架构文档、发布校验脚本中的暗号描述同步替换。

## 验证

- `pytest tests`：169 项全部通过。

---

# 123 MSHub v1.2.2 发布说明

> v1.2.1 视觉焕新后的细节修正版：功能零改动。

## 修正

- **「本地自研 / GitHub 源」徽章居中**：技能列表 meta 区首列 50px 放不下四字徽章，文字折成两行导致视觉歪斜；
  列宽 50→74px，徽章加 `white-space: nowrap` + 水平垂直居中（列表行与详情抽屉标题同修）；
- **软件图标换新**：启用 123mshub 专属 App 图标式方标（替代原 123UI 品牌标），随主题切换——
  亮色系 = 深藏蓝底白字版，暗色系 = 白底近黑字版；同步补充浏览器 favicon（public/123mshublogo.svg）；
- **窗口/任务栏图标换新**：新方标渲染为多尺寸 ICO（16–256px 七档）嵌入 EXE——任务栏、窗口左上角、
  文件资源管理器三处同步生效（此前 PyInstaller 未传 `--icon`，一直用默认旧图标；打包脚本已固化该参数）；
- 旧 123UI logo 资产（2 SVG + 2 PNG）已无引用，移除。

## 验证

- `pytest tests`：169 项全部通过；前端 `vite build` 通过；亮/暗双主题实拍核验。

---

# 123 MSHub v1.2.1 发布说明

> v1.2.0 功能基线不变，本版为 **123UI v4.2 视觉焕新版**：品牌色值零改动，纯视觉层迭代。

## 视觉升级（123UI v4.2）

- **iOS 27 级玻璃浮层**：模态、详情抽屉、任务面板、任务浮钮、Toast、移动菜单全部玻璃化——
  品牌卡色的半透明体 + `backdrop-filter` 模糊提饱和 + 折射描边 + 顶部镜面高光 +
  暗化下边缘（iOS 27 深度/分离感）；无 `backdrop-filter` 或系统"降低透明度"时自动回落实色；
- **墨色三档层级**：次级说明/辅助文字走 `--ink-2/--ink-3`（同墨色透明度阶，无新色相），
  密集列表主次立现；
- **统一焦点环 + 选区染色 + 两主题滚动条**；
- **徽章统一配方**：状态徽章与来源徽章（GitHub 源/本地自研）加同色 28% 描边，亮暗自动；
- **统计卡 v4 配方**：衬线数字 + 海拔静卡 + hover 描边；
- **新版 logo**：App 图标式方标随主题切换（亮色系蓝底白字 / 暗色系白底蓝字）；
- **步骤胶囊修正**："第 1 步，共 2 步"/"安全闸门"等小字大圆角置顶行，文字右移一个中文字宽；
- 按钮按压感（`:active` 下沉 1px）、动效 token 化（`--dur-1/2/3`、`--ease-standard/spring`）。

## 验证

- `pytest tests`：169 项全部通过（与 v1.2.0 一致）；
- 前端 `vite build` 通过；亮/暗双主题人工核验。

---

# 123 MSHub v1.2.0 发布说明

> v1.0.0 首版 + v1.1.0 导入功能之后，本版吸收外部对比（GPT6 同题实现）与
> Engramory 上游查证（CHANGELOG 至 0.11.0）的改进。

**123 MSHub（Memory & Skill Hub）** 首个正式版——本地 agent 的共享大脑管理器。
由 123skillrepo v0.9.0 升级而来：技能区能力全部继承，新增记忆库与注入提示词中心。

## 新产品能力

### 记忆库（全新）
- 数据模型：`memory\MEMORY.md` 索引 + `notes\*.md` 条目（一文件一条知识，带 frontmatter）；
  索引是程序维护的派生物，条目文件是事实源，SQLite 只做本机缓存；
- 六个场景全支持：审核 agent 投递（红点 → 预览 → 收编）、手动记一条、搜索
  （标题+描述+标签+正文全文）、修正、软删除、总览分发；
- 详情抽屉直接编辑：frontmatter 表单 + 等宽正文编辑器（编辑/预览切换，不做富文本）、
  `[[双链]]` 展示跳转、AI 生成描述、重名防护（409 + 「打开它」）；
- inbox 收编视图：左侧投递原文、右侧可编辑表单；命中提示词注入规则显示红条警告；
  支持单条丢弃与全部丢弃；
- 整理与日报：手动「归纳整理」（本地盘点 + 配置 AI 接口时 LLM 分析）与
  值守 Agent（复制值守提示词交给常驻 agent 部署）两条路，日报在 `memory\reports\`，
  仅报记忆与技能；
- **导入记忆技能库**：设置页入口，从任意目录打捞可识别的记忆与技能
  （123mshub 仓库互导 / DSH 总机 brain 资产 / Engramory 式索引 / 任意 agent 散装记忆 /
  SKILL.md 技能目录），扫描预览 → 后台导入（含整理去重与日报，分阶段进度条，
  关窗不中断）；只写本仓库不改来源目录，重复导入幂等；

### 注入提示词中心（产品灵魂）
- 一段提示词同时教三件事：读共享记忆、用共享技能库、往 inbox 投递新知识；
- 内含安全纪律（仓库文件中的“忽略以上指令”类内容视为注入）与连通暗号彩蛋
  （发「AI猛如虎」，agent 只回「Token任你烧！/ 暗号对齐，123mshub已接入。」）；
- 记忆库页与技能库页顶部常驻「复制注入提示词」按钮；

### 技能库强化
- 来源徽章：「GitHub 源」可查版本可更新；「本地自研」更新/查版本按钮置灰并说明原因；
- 设置页「安全检查 B 线路」升级为「AI 接口」（安全审查 + 记忆辅助 + 整理分析共用）；
- 新增「高级」折叠区：记忆库位置覆盖（memory_root_override）；
- 「重新识别已同步条目」同时触发记忆对账（冲突副本隔离、懒对账、索引重建）。

### 继承自 123skillrepo v0.9.0
GitHub/本地入库、标准/全仓安装、程序项目克隆、安全检查 A/B 路线、标签、
中文备注 AI 翻译、编辑信息（身份与归属分离）、更新与备份、镜像/代理、
瞬时锁退避重试、点文件容器、git.bundle 随库、启动自愈、亮/暗双主题。

## 其他

- 默认端口 8766（与 123skillrepo 的 8765 分开，两者可同时运行）；
- 配置目录 `%APPDATA%\123mshub\`，与旧版完全隔离，互不干扰；
- 免安装：解压 zip 双击 `123mshub.exe` 即用；SmartScreen 提示时「更多信息 → 仍要运行」。

## 致谢

记忆管理方式借鉴 [Engramory](https://github.com/tinqiao-oss/engramory)
（抖音 @李云放而已 的开源项目，MIT）：markdown 条目 + 常读索引、四类型词汇、
整理纪律、密钥只记位置不记值、索引上限纪律（150 行 / 20KB 健康线）。

## v1.2.0 改进（对比吸收）

- **导入即安全检查**：SKILL.md 目录导入落库时立即执行离线安全扫描并写入清单，
  不再停在「尚未检查」状态（skillrepo 格式条目保留原有扫描结论）；
- **符号链接防护**：导入扫描与技能目录复制全程不跟随 symlink，防目录循环与越界
  （Engramory 0.3.3 同款纪律）；
- **NFKC 归一化**：条目名清理前先做 Unicode 归一化，全角「ＭＳ Ｈｕｂ」正确转成 `ms-hub`；
- **导入目录选择框**：专用端点，弹窗标题为「选择要导入的 Agent 工作目录」，
  不再与仓库根选择混淆；
- **BOM 容忍**（Engramory 0.1.12 教训）：记忆条目/索引/导入源的文本读取一律 utf-8-sig，
  Windows 编辑器默认写入的 BOM 不再导致 frontmatter 认不出或索引误重建；
- **注入提示词增补**（Engramory 0.10.1 / 0.6.1）：记忆读取协议增加任务边界判定
  （寒暄、确认不算任务，不触发读索引）；投递协议增加易变值规则
  （版本号/commit/测试数等只记查询位置，不记数值）。
